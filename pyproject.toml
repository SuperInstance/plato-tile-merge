[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-merge"
version = "0.1.0"
description = "Tile merging with conflict resolution"
license = {text = "MIT"}
requires-python = ">=3.8"
